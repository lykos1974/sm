# AB=CD Geometry Input Diagnostic

## Scope

Research infrastructure diagnostic only.

- No AB=CD logic was modified.
- No geometry calculations were run.
- No expectancy, detector, strategy, entry/exit, or future price outcome logic was run.
- No placeholder geometry outputs were generated by this diagnostic.

## Scripts Compared

- `research_v2/patterns/pnf_abcd_population_audit.py`
- `research_v2/patterns/pnf_abcd_geometry_audit.py`

## Population Audit Input Discovery

### Default root in the current script

`pnf_abcd_population_audit.py` defines:

```text
TRUSTED_INPUT_ROOT = research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v2
REACTIONS_FILENAME = harmonic_reactions_by_threshold.csv
```

### Exact path checks performed

The population audit calls `_trusted_reactions_path(input_root)`, which checks exactly these paths, in order:

1. `<input_root>/harmonic_reactions_by_threshold.csv`
2. `<input_root>/audit/harmonic_reactions_by_threshold.csv`

Using the current default root, that means:

1. `research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v2/harmonic_reactions_by_threshold.csv`
2. `research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v2/audit/harmonic_reactions_by_threshold.csv`

If the population audit is explicitly run with:

```text
--input-root research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v3
```

then it checks exactly:

1. `research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v3/harmonic_reactions_by_threshold.csv`
2. `research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v3/audit/harmonic_reactions_by_threshold.csv`

## Geometry Audit Input Discovery

### Default roots in the current script

`pnf_abcd_geometry_audit.py` defines:

```text
TRUSTED_PIVOT_ROOT = research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v3
TRUSTED_POPULATION_ROOT = research_v2/patterns/abcd_population_local_v2
REACTIONS_FILENAME = harmonic_reactions_by_threshold.csv
```

### Exact path checks performed

The geometry audit first calls `_population_candidate_path(population_root)`, which checks exactly these paths, in order:

1. `research_v2/patterns/abcd_population_local_v2/abcd_population_candidates.csv`
2. `research_v2/patterns/abcd_population_local_v2/abcd_candidates.csv`
3. `research_v2/patterns/abcd_population_local_v2/abcd_candidate_population.csv`

If none of those files exists, the geometry audit then calls `_trusted_reactions_path(pivot_root)`, which checks exactly these paths, in order:

4. `research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v3/harmonic_reactions_by_threshold.csv`
5. `research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v3/audit/harmonic_reactions_by_threshold.csv`

## Files Found in This Workspace

The diagnostic filesystem check found:

```text
FOUND   research_v2/patterns/harmonic_swing_threshold_local_v2/harmonic_reactions_by_threshold.csv
MISSING research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v2/harmonic_reactions_by_threshold.csv
MISSING research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v2/audit/harmonic_reactions_by_threshold.csv
MISSING research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v3/harmonic_reactions_by_threshold.csv
MISSING research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v3/audit/harmonic_reactions_by_threshold.csv
MISSING research_v2/patterns/abcd_population_local_v2/abcd_population_candidates.csv
MISSING research_v2/patterns/abcd_population_local_v2/abcd_candidates.csv
MISSING research_v2/patterns/abcd_population_local_v2/abcd_candidate_population.csv
```

## First Failing Condition

The first geometry-audit discovery condition that fails is:

```text
research_v2/patterns/abcd_population_local_v2/abcd_population_candidates.csv is not a file
```

The geometry audit then continues through the remaining population-candidate names and then the validated pivot file checks. All approved geometry input checks fail in this workspace.

## Exact Rejection Reason

The blocking message is produced when `_trusted_reactions_path(pivot_root)` returns `None` after these two validated-pivot checks both fail:

```text
research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v3/harmonic_reactions_by_threshold.csv
research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v3/audit/harmonic_reactions_by_threshold.csv
```

The exact rejection reason is:

```text
missing trusted harmonic_reactions_by_threshold.csv under research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v3 or research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v3/audit
```

## Precise Root Cause

The geometry audit blocks because, in the current workspace, the approved validated input path it checks does not exist as either of the two accepted file locations:

```text
research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v3/harmonic_reactions_by_threshold.csv
research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v3/audit/harmonic_reactions_by_threshold.csv
```

The only similarly named reactions file found by this diagnostic is not under the approved `VALIDATED_harmonic_swing_threshold_local_v3` root:

```text
research_v2/patterns/harmonic_swing_threshold_local_v2/harmonic_reactions_by_threshold.csv
```

Therefore, the population audit can only succeed with `research_v2/patterns/VALIDATED_harmonic_swing_threshold_local_v3` if that directory/file is present in the runtime workspace or supplied through `--input-root`. In this workspace snapshot, that validated v3 artifact is not present, so the geometry audit's block is caused by missing approved input files, not by AB=CD geometry logic.
