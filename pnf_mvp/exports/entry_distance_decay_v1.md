# Entry Distance Decay Study v1

The export script is available at `experiments/entry_distance_decay_study.py`, but this checkout does not include `pnf_mvp/exports/pattern_taxonomy_audit_v2.csv` or `pnf_mvp/data/pnf_mvp_research_clean.sqlite3`, so the data-backed export could not be generated here.

Run:

```bash
python experiments/entry_distance_decay_study.py
```

When the required research inputs are present, the command overwrites this placeholder with the grouped entry-distance decay summary.
