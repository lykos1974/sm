# ROS-1 Registries

This folder contains minimal registry folders and templates. Each registry stores plain-text records that preserve research lineage without executing research or trading logic.

Required lineage: Observation → Belief → Experiment/Hypothesis → Validation Run → Evidence → Decision → Knowledge.

Do not store executable code, trading logic, datasets, exports, sqlite/parquet files, or detector implementations here.
